<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="login_et_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginEnd">40dp</item>
        <item name="android:layout_marginLeft">40dp</item>
        <item name="android:layout_marginRight">40dp</item>
        <item name="android:layout_marginStart">40dp</item>
        <item name="android:textColor">@color/text_gray</item>
        <item name="android:background">@drawable/login_input_bg_selector</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:padding">5dp</item>
    </style>

    <style name="activity_main_bottom_style">
        <item name="android:layout_weight">1</item>
        <item name="android:padding">12dp</item>
        <item name="android:background">?selectableItemBackground</item>

    </style>

    <style name="activity_main_mine_line_menu_style">
        <item name="android:padding">15dp</item>
        <item name="is_left_icon_visible">true</item>
        <item name="left_icon_size">25dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/ripple_bg_white</item>
    </style>

    <style name="activity_main_index_input_location_text_style">
        <item name="android:layout_width">120dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">10dp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:background">@drawable/disable_edit_text_bg</item>
    </style>
</resources>
